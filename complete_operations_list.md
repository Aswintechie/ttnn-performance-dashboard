# Complete TTNN Operations List (Alphabetical)

**Total Operations: 288**

## 📋 **All Operations (A-Z)**

```
abs, abs_bw, absolute_difference, acos, acos_bw, acosh, acosh_bw, add, 
add_, add_bw, addalpha, addalpha_bw, addcdiv, addcdiv_bw, addcmul, 
addcmul_bw, angle, asin, asin_bw, asinh, asinh_bw, assign, atan, 
atan_bw, atan2, atan2_bw, atanh, atanh_bw

bias_gelu, bias_gelu_, bias_gelu_bw, bitwise_and, bitwise_not, 
bitwise_or, bitwise_xor

cbrt, ceil, ceil_bw, celu, clamp, clip, clip_binary, complex_abs, 
complex_recip, complex_tensor, conj, cos, cos_bw, cosh, cosh_bw, 
cumsum, cumprod

deg2rad, deg2rad_bw, digamma, digamma_bw, div_, divide, divide_, 
divide_bw

elu, eq, eq_, eqz, erf, erf_bw, erfc, erfc_bw, erfinv, erfinv_bw, 
exp, exp_bw, exp2, exp2_bw, expm1, expm1_bw

fill, fill_bw, fill_zero_bw, floor, floor_bw, fmod, fmod_bw, frac, 
frac_bw

ge, ge_, geglu, gelu, gelu_bw, gez, glu, gt, gt_, gtz

hardshrink, hardshrink_bw, hardsigmoid, hardsigmoid_bw, hardswish, 
hardswish_bw, hardtanh, heaviside, hypot, hypot_bw

i0, i0_bw, i1, identity, imag, isclose, isfinite, isinf, isnan, 
isneginf, isposinf

ldexp, ldexp_, ldexp_bw, le, le_, leaky_relu, lerp, lerp_bw, lez, 
lgamma, lgamma_bw, log, log_bw, log_sigmoid, log_sigmoid_bw, log1p, 
log1p_bw, log10, log10_bw, log2, log2_bw, logaddexp, logaddexp_, 
logaddexp_bw, logaddexp2, logaddexp2_, logaddexp2_bw, logical_and, 
logical_and_, logical_not, logical_not_, logical_or, logical_or_, 
logical_xor, logical_xor_, logit, logit_bw, ltz

mac, max, max_bw, maximum, mean, mean_bw, min, min_bw, minimum, mish, 
mul, mul_, mul_bw, multigammaln, multigammaln_bw, multiply, multiply_

ne, ne_, neg, neg_bw, nextafter, nez

polar, polyval, pow, pow_bw, prod, prod_bw, prelu

rad2deg, rad2deg_bw, rdiv, real, reciprocal, reciprocal_bw, reglu, 
relu, relu_bw, relu6, relu6_bw, relu_max, relu_min, remainder, 
remainder_bw, round, round_bw, round_binary, rpow, rsqrt, rsqrt_bw, 
rsub_, rsub_bw

selu, selu_bw, sign, sign_bw, signbit, sigmoid, sigmoid_bw, 
sigmoid_accurate, silu, silu_bw, sin, sin_bw, sinh, sinh_bw, softplus, 
softshrink, softshrink_bw, softsign, softsign_bw, sqrt, sqrt_bw, 
square, square_bw, squared_difference, squared_difference_, 
squared_difference_bw, std, std_bw, std_hw, sub, sub_, sub_bw, 
subalpha, subalpha_bw, subtract, subtract_, swiglu, swish

tan, tan_bw, tanh, tanh_bw, tanhshrink, tanhshrink_bw, threshold, 
tril, triu, trunc, trunc_bw

unary_chain

var, var_bw, var_hw

where, where_bw

xlogy, xlogy_bw
```

## 📊 **Category Breakdown**
- **Unary Operations**: 98 operations  
- **Binary Operations**: 64 operations
- **Ternary Operations**: 5 operations
- **Reduction Operations**: 9 operations  
- **Backward Operations**: 80 operations
- **Complex Operations**: 8 operations

**Total: 288 operations** ✅ 